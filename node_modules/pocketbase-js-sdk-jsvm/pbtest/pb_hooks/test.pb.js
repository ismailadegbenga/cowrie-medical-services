/// <reference path="../../src/types.d.ts" />

routerAdd("GET", "/test", (e) => {
    try {
        sleep(1000);
        console.log(`pb client test`);
        const Client = require(`${__hooks}/pocketbase.cjs`);

        const res = $http.send({
            url: "http://127.0.0.1:8090/api/health",
        });
        e.json(200, res);
    } catch (e) {
        console.error(`${e}`);
    }
});

// const pb = new Client(`http://localhost:8090`);

// const res = pb.health.check();
// console.log(JSON.stringify(res, null, 2));

// console.log(`done`);
